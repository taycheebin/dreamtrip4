class Trip{
  final String tripTitle;
  final DateTime startDate;
  final DateTime endDate;
  final double budget;
  final String travelType;

  Trip(this.tripTitle, this.startDate, this.endDate, this.budget, this.travelType);
}